legacy-peer-deps=true
# Temporary fix for ESLint 9.x + TypeScript ESLint compatibility
# This allows npm to install packages with conflicting peer dependencies
# Remove this after package-lock.json is updated with correct versions 