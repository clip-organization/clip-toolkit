{
  "name": "@clip-toolkit/validator-core",
  "version": "0.1.0",
  "description": "Core validation and schema management for CLIP (Context Link Interface Protocol)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --watch",
    "test": "jest",
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "typescript": "^4.9.5"
  },
  "devDependencies": {
    "jest": "^29.5.0",
    "eslint": "^8.44.0"
  }
} 